package me.ipLogin.methods;
/**
 * 
 * @deprecated
 *
 */
public class helper {

	/*
	 
	 Bukkit.getBanList(Type.IP).addBan(ip, "reason", new Date(), "admin"); //if type ip lazm ip etc
	 
	 public void setBanned(OfflinePlayer player, String reason, Date date) {
		 Bukkit.getBanList(Type.NAME).addBan(player.getName(), reason, date, null);
	 }
	 
	 public void setUnBanned(OfflinePlayer player) {
		 Bukkit.getBanList(Type.NAME).getBanEntry(player.getName()).setExpiration(new Date(System.currentTimeMillis()));
	 }
	 
	 */
}
